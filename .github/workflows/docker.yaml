name: Docker Build Image
on: push
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello world
        run: echo "Hello World"
      - name: Echo where
        run: pwd
      - name: Echo dir
        run: ls -lah
